apiVersion: v1
kind: Service
metadata:
    name: "kubet-cee4"
    labels:
        app: "kubet-cee4"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "kubet-cee4"